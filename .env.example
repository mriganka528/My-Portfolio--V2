# Add your PostgreSQL connection string here when it is available.
# For verified TLS connections, use sslmode=verify-full.
DATABASE_URL=""
# The exact public origin, without a trailing slash. Use HTTPS in production.
APP_URL="http://localhost:3000"
# Keep false locally. Only enable if your reverse proxy overwrites X-Forwarded-For.
TRUST_PROXY="false"
# Optional, used only by the admin provisioning script. No default credentials.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
